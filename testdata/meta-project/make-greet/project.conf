# Project-level variable overrides for the make-greet fixture.
# See manual-greet/project.conf for context: prefix=/usr is the
# FDSDK-shape override of BuildStream stock /usr/local that
# rebases every derived path under /usr.
variables:
  prefix: /usr
