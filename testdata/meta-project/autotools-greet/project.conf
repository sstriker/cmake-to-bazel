# Project-level variable overrides for the autotools-greet fixture.
# prefix=/usr matches FDSDK's project.conf overlay of BuildStream
# stock /usr/local; without it the fixture would install under
# /usr/local and the gate's `usr/bin/greet` assertion would miss.
variables:
  prefix: /usr
