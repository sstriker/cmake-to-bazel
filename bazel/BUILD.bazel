"""Public surface for the cmake-to-bazel envelope's Starlark.

Downstream Bazel projects load `converted_pkg_repo.bzl` via the
`@cmake_to_bazel//bazel:converted_pkg_repo.bzl` label.
"""

exports_files(
    ["converted_pkg_repo.bzl"],
    visibility = ["//visibility:public"],
)
